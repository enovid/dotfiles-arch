[user]
	email = emahn.novid@gmail.com
	name = enovid
[credential]
	helper = cache --timeout=600000
[filter "lfs"]
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
[hub]
	protocol = https
[init]
	defaultBranch = main
